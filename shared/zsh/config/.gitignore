# Machine-local override files. Sourced by the base/interactive loaders but
# never committed — same spirit as the old `x-*` pattern.
base.x-*.zsh
int.x-*.zsh
# Bare-named compat symlinks that x-* files reference as siblings (e.g.
# int.x-fast-bake.zsh sources ./x-work-secrets.zsh). Not auto-sourced.
x-*.zsh
*.local.zsh
